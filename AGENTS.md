# Agent Instructions for Zahra Vs

## Project Overview
- This workspace currently contains a single static HTML project: `index.html`.
- No build system, package manager, or test framework is present.
- The project appears to be a small static website or prototype.

## Guidance for AI coding agents
- Focus on editing or expanding the static site in `index.html`.
- Do not assume frameworks, build tools, or package dependencies unless the user adds them.
- Use standard HTML/CSS/JavaScript conventions for any new files or enhancements.
- If additional files such as CSS, JS, or assets are needed, create them alongside `index.html`.

## When to ask the user
- If a request requires a framework, build setup, or tooling, ask whether they want a static site or a structured app.
- If a new file or directory structure is needed, confirm naming and organization.

## Notes
- Because the workspace is currently empty aside from `index.html`, avoid introducing unnecessary complexity.
