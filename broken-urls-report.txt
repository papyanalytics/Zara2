This report is stale.

Use the validation utilities under `scripts/` to regenerate an up-to-date asset audit:
- `scripts\check_asset_paths.ps1` for broken asset references in HTML, JS, and CSS
- `map_images.py` for a summary of referenced and unused product image files

Remove or overwrite this file after validation if it is no longer needed.
